import { SafeAreaView, StyleSheet, Text, TextInput, View } from "react-native";

const LoginPage = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};
export default LoginPage;
